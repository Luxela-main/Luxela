export const metadata = {
  title: 'Order Details | Buyer Dashboard',
  description: 'View detailed information about your order including tracking and delivery status.',
};

export default function OrderDetailsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}